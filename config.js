/**
 * Reward Geni Documentation — Configuration
 * 
 * Change BASE_URL to match your deployment environment.
 * This is the ONLY file you need to edit when deploying to a new domain.
 * 
 * Examples:
 *   Staging:     "https://staging.rewardgenix.co.in"
 *   Production:  "https://api.rewardgenix.com"
 *   GitHub Pages: "https://YOUR_USERNAME.github.io/YOUR_REPO"
 */

var CONFIG = {
  // The API base URL shown in documentation code examples
  BASE_URL: "https://staging.rewardgenix.co.in",

  // Site metadata (used in page titles and meta tags)
  SITE_NAME: "Reward Geni",
  API_VERSION: "v1.0.0"
};
